// Package main is a placeholder for the fixture's embedded-copy source tree.
// Never built -- the fixture's .bin/ prebuilt (placed there at verify.sh run
// time) is what bootstrap.sh's Block 2 actually runs before the swap.
package main

func main() {}
